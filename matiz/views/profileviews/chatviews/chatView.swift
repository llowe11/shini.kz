import SwiftUI

struct chatView: View {
    var body: some View {
        ZStack(alignment: .top){
            ScrollView{
                Text("coming soon...")
            }.padding(.top,350)
            chatheaderView()
        }
    }
}
