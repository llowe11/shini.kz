import SwiftUI

struct profileheaderView: View {
    var body: some View {
        Text("Кабинет")
            .padding()
                .foregroundColor(.black)
                .frame(maxWidth: .infinity)
                .padding(.top, 35)
                .background(Color.white)
    }
}

