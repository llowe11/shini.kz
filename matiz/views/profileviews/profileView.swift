import SwiftUI

struct profileView: View {
    var body: some View {
        ZStack(alignment: .top){
            ScrollView{
                VStack{
                    Text("content")
                }
            }
            profileheaderView()
        }
    }
}

