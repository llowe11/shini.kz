import SwiftUI

struct chatView: View {
    var body: some View {
        ZStack(alignment: .top){
            ScrollView{
                Text("test")
            }
            chatheaderView()
        }
    }
}
