import SwiftUI

struct carfeedView: View {
    var body: some View {
        VStack{
            carTile()
            carTile()
            carTile()
        }
    }
}

#Preview {
    carfeedView()
}
